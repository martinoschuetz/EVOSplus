#include <stdio.h>
#include <stdlib.h>

#define OFFSET 1 /* wenn das array von 1 bis n definiert ist, ist der OFFSET 0
		    wenn es von 0 bis n-1 definiert ist, dann ist der OFFSET 1*/
typedef double KEY;
typedef struct{
   double key;
   int  ptr;
} ARRTYPE;
